# gridifyClass class

Class for creating a gridify object.

## Slots

- `object`:

  A grob like object.

- `layout`:

  A gridifyLayout object.

- `elements`:

  A list of text elements, calls to
  [`set_cell()`](https://pharmaverse.github.io/gridify/reference/set_cell.md).
